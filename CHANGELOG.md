# Changelog

## Unreleased

- Fix `RenderSliverAdvancedGrid` parent data handling to avoid type cast crashes under `SliverPadding`, including null-check and semantics regressions; added widget and render regression tests.
